run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - gosimple
    - ineffassign
    - misspell
    - gofmt

linters-settings:
  errcheck:
    check-blank: true

issues:
  exclude-use-default: true
